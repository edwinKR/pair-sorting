let swapCounter = 0;
let comparisonCounter = 0;

function bubbleSort(array) {
  /* your code here */

  //swap() will be invoked somewhere here.
  return array;
}

//swapping function
function swap() {
  swapCounter++;
  return '!';
}
